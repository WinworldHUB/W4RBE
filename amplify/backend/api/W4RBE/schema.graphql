type Product @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  body: String
  category: String
  size: String
  quantity: Int
  price: Float
  taxable: Boolean
  featuredImage: String!
  otherImages: [String]
  published: Boolean!
}
 